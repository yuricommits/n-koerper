[package]
name = "n-koerper"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.32.1"
minifb = "0.28.0"
rayon = "1.11.0"

[profile.release]
lto = true            # link-time optimization, catches cross-crate optimizations
codegen-units = 1     # slower compile but better optimized binary
